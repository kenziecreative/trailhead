---
phase: 02-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scaffold/templates/settings.json.tmpl
  - scaffold/settings.json
  - skills/knzinit/SKILL.md
autonomous: true
requirements: [SETT-01, SETT-02, SETT-03, SETT-04, SETT-05, SETT-06]

must_haves:
  truths:
    - "Generated settings.json includes $schema key and IDE validation works"
    - "Generated settings.json always denies write/edit access to .env and secrets files regardless of project type"
    - "Code projects get allow rules for build/test commands; non-code projects get includeGitInstructions:false and keep-coding-instructions:false"
    - "Generated settings.json includes env block with KNZINIT_PROJECT_TYPE and KNZINIT_VERSION"
    - "Generated settings.json sets plansDirectory to .planning/plans"
  artifacts:
    - path: "scaffold/templates/settings.json.tmpl"
      provides: "Full settings template with all SETT requirements — code/unsure variant"
      contains: "$schema"
    - path: "skills/knzinit/SKILL.md"
      provides: "Updated orchestrator with expanded Step 4 merge logic and non-code conditional adaptation"
      contains: "keep-coding-instructions"
  key_links:
    - from: "skills/knzinit/SKILL.md"
      to: "scaffold/templates/settings.json.tmpl"
      via: "SKILL.md Step 4 references template for merge source"
      pattern: "settings.json.tmpl"
    - from: "scaffold/templates/settings.json.tmpl"
      to: "scaffold/settings.json"
      via: "Template replaces the old hooks-only file as the canonical source"
      pattern: "permissions"
---

<objective>
Create the expanded settings.json template with full platform coverage (schema validation, security deny rules, project-type allow rules, environment metadata, plans directory) and update the SKILL.md orchestrator with merge logic and non-code conditional adaptation.

Purpose: This is the entire Settings phase — expanding knzinit from generating hooks-only settings to generating comprehensive, project-type-aware settings.json files.
Output: settings.json.tmpl template, updated SKILL.md Step 4, cleaned up old scaffold/settings.json.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-settings/02-CONTEXT.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs from existing codebase -->

From scaffold/settings.json (current hooks-only template to be replaced):
```json
{
  "hooks": {
    "PreCompact": [{ "matcher": "", "hooks": [{ "type": "command", "command": "bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/pre-compact-check.sh\"", "timeout": 10 }] }],
    "Stop": [{ "matcher": "", "hooks": [{ "type": "command", "command": "bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/milestone-check.sh\"", "timeout": 10 }] }],
    "PreToolUse": [{ "matcher": "Bash", "hooks": [{ "type": "command", "command": "bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/pre-commit-secrets.sh\"", "timeout": 10 }] }]
  }
}
```

From scaffold/resolve-root.sh (provides version for env block):
```bash
export KNZINIT_VERSION  # version string from plugin.json
knzinit_version_marker()  # returns "<!-- knzinit vX.Y.Z -->"
```

From skills/knzinit/SKILL.md Step 4 (current merge logic, line ~142-147):
```markdown
## Step 4: Merge Settings
If `.claude/settings.json` already exists, READ it first and merge the new hooks into the existing structure. Never overwrite existing hooks -- add alongside them.
If it doesn't exist, create it fresh with all applicable hooks.
```

From skills/knzinit/SKILL.md Step 1 (project type question, line ~16):
```markdown
2. **Is this a code project, a non-code project, or not sure yet?**
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create expanded settings.json.tmpl and remove old scaffold/settings.json</name>
  <files>scaffold/templates/settings.json.tmpl, scaffold/settings.json</files>
  <action>
Create `scaffold/templates/settings.json.tmpl` as a valid JSON file representing the code/unsure project variant (the full template). This is the canonical settings template — all sections present.

The template must contain these sections in this order:

1. **$schema** (SETT-01): `"$schema": "https://json.schemastore.org/claude-code-settings.json"` as the first key.

2. **permissions.deny** (SETT-02): Array of deny rules for secrets files. Each rule is an object with `filePath` and `operations` keys. Deny both Write and Edit operations for these patterns:
   - `.env*`
   - `**/*.pem`
   - `**/*.key`
   - `**/*credentials*`
   - `**/*secret*`
   - `**/id_rsa*`
   - `**/id_ed25519*`

3. **permissions.allow** (SETT-03): Array of allow rules for common build/test commands. Each rule is an object with `command` key. Include these commands:
   - `npm *`
   - `yarn *`
   - `pnpm *`
   - `bun *`
   - `npx *`
   - `pytest *`
   - `cargo *`
   - `go test *`
   - `go build *`
   - `make *`

4. **env** (SETT-05): Object with two keys:
   - `"KNZINIT_PROJECT_TYPE": "code"` (placeholder — SKILL.md sets actual value at scaffold time: code | noncode | unknown)
   - `"KNZINIT_VERSION": "{{VERSION}}"` (placeholder — SKILL.md substitutes from plugin.json)

5. **plansDirectory** (SETT-06): `"plansDirectory": ".planning/plans"`

6. **hooks**: Preserve the exact same hooks structure from the current `scaffold/settings.json` (PreCompact, Stop, PreToolUse).

After creating the template, **delete** `scaffold/settings.json` (the old hooks-only file). The template fully replaces it.

Do NOT include `includeGitInstructions` or `keep-coding-instructions` in the template — those are non-code-only flags that SKILL.md adds conditionally (Task 2).
  </action>
  <verify>
    <automated>test -f scaffold/templates/settings.json.tmpl && ! test -f scaffold/settings.json && python3 -c "import json; d=json.load(open('scaffold/templates/settings.json.tmpl')); assert '\$schema' in d; assert 'deny' in d.get('permissions',{}); assert 'allow' in d.get('permissions',{}); assert 'KNZINIT_PROJECT_TYPE' in d.get('env',{}); assert d.get('plansDirectory')== '.planning/plans'; assert 'hooks' in d; print('PASS')"</automated>
  </verify>
  <done>scaffold/templates/settings.json.tmpl exists with valid JSON containing $schema, permissions.deny (7 patterns), permissions.allow (10 commands), env block, plansDirectory, and hooks. Old scaffold/settings.json is deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Update SKILL.md Step 4 with expanded merge logic and non-code conditional adaptation</name>
  <files>skills/knzinit/SKILL.md</files>
  <action>
Read the current `skills/knzinit/SKILL.md` and rewrite **Step 4: Merge Settings** to handle the expanded template. The new Step 4 must cover:

**Template source change:** Update any references from `scaffold/settings.json` to `scaffold/templates/settings.json.tmpl` (check Step 3B around line 118 where it says "using the pattern in `${KNZINIT_ROOT}/scaffold/settings.json`" — update that path too).

**Step 4 expanded merge logic (merge-not-overwrite):**

When `.claude/settings.json` already exists:
- **hooks**: Append new hooks to existing hook arrays (same as before, but stated explicitly)
- **permissions.deny**: Union of existing deny rules + template deny rules (no duplicates)
- **permissions.allow**: Union of existing allow rules + template allow rules (no duplicates)
- **env**: Existing keys preserved, new keys added (KNZINIT_PROJECT_TYPE, KNZINIT_VERSION)
- **Scalar keys** ($schema, plansDirectory): Set if not already present; do not overwrite existing values

When `.claude/settings.json` does not exist:
- Create fresh from the template

**Non-code conditional adaptation (SETT-03, SETT-04):**

After the merge logic section, add a clearly labeled subsection:

"**Project-type adaptation:** After merging, apply these adjustments based on the project type from Step 1:"

- **Code or "not sure yet":** Use the template as-is (it already represents the code variant)
- **Non-code:** Remove the entire `permissions.allow` array (non-code projects don't run build/test commands). Add two keys at the top level:
  - `"includeGitInstructions": false`
  - `"keep-coding-instructions": false`
  - Set `env.KNZINIT_PROJECT_TYPE` to `"noncode"`

- **All project types:** Substitute `{{VERSION}}` in `env.KNZINIT_VERSION` with the actual version from plugin.json (use KNZINIT_VERSION from resolve-root.sh). Set `env.KNZINIT_PROJECT_TYPE` to the normalized value: `code`, `noncode`, or `unknown`.

**Step 6 addition (SETT-03, SETT-04):** In the Step 6 Report section, add a note that for non-code projects the report should include: "Git instructions disabled. If you use git for version control, set includeGitInstructions:true in .claude/settings.json."

Keep all other SKILL.md content unchanged. The edits are surgical: Step 3B path reference, Step 4 rewrite, Step 6 non-code note.
  </action>
  <verify>
    <automated>grep -q "settings.json.tmpl" skills/knzinit/SKILL.md && grep -q "permissions.deny" skills/knzinit/SKILL.md && grep -q "permissions.allow" skills/knzinit/SKILL.md && grep -q "keep-coding-instructions" skills/knzinit/SKILL.md && grep -q "includeGitInstructions" skills/knzinit/SKILL.md && grep -q "KNZINIT_PROJECT_TYPE" skills/knzinit/SKILL.md && grep -q "plansDirectory" skills/knzinit/SKILL.md && grep -q "noncode" skills/knzinit/SKILL.md && echo "PASS"</automated>
  </verify>
  <done>SKILL.md Step 4 expanded with full merge logic (hooks, permissions.deny, permissions.allow, env, scalar keys), non-code conditional adaptation (remove allow, add includeGitInstructions:false + keep-coding-instructions:false), version/project-type substitution, and Step 6 non-code report note. Template path references updated from scaffold/settings.json to scaffold/templates/settings.json.tmpl.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full phase:

1. Template validity: `python3 -c "import json; json.load(open('scaffold/templates/settings.json.tmpl'))"`
2. Old file removed: `! test -f scaffold/settings.json`
3. SKILL.md references new template path: `grep -c "settings.json.tmpl" skills/knzinit/SKILL.md` returns >= 1
4. All six SETT requirements addressed:
   - SETT-01: `grep '$schema' scaffold/templates/settings.json.tmpl`
   - SETT-02: `grep 'deny' scaffold/templates/settings.json.tmpl`
   - SETT-03: `grep 'allow' scaffold/templates/settings.json.tmpl` and `grep 'includeGitInstructions' skills/knzinit/SKILL.md`
   - SETT-04: `grep 'keep-coding-instructions' skills/knzinit/SKILL.md`
   - SETT-05: `grep 'KNZINIT_PROJECT_TYPE' scaffold/templates/settings.json.tmpl`
   - SETT-06: `grep 'plansDirectory' scaffold/templates/settings.json.tmpl`
</verification>

<success_criteria>
- settings.json.tmpl is valid JSON with all 6 required sections
- Old scaffold/settings.json is deleted
- SKILL.md Step 4 handles merge-not-overwrite for all sections
- SKILL.md conditionally adapts for non-code projects (removes allow, adds behavior flags)
- SKILL.md substitutes version and project type at scaffold time
- All SETT-01 through SETT-06 requirements are addressed
</success_criteria>

<output>
After completion, create `.planning/phases/02-settings/02-01-SUMMARY.md`
</output>
