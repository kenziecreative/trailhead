---
phase: 04-non-code-and-skills
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scaffold/templates/CLAUDE.md.tmpl
  - scaffold/templates/STATE.md.tmpl
  - scaffold/skills/sanity-check/SKILL.md
autonomous: true
requirements: [NCODE-01, NCODE-03, NCODE-04]

must_haves:
  truths:
    - "CLAUDE.md.tmpl has non-code conditional sections (Work Stages, Session Patterns) that replace Project Structure"
    - "CLAUDE.md.tmpl includes Session Discipline reminder for non-code projects"
    - "STATE.md.tmpl has non-code variant open items (deliverables, recurring decisions) instead of tech stack"
    - "sanity-check reads KNZINIT_PROJECT_TYPE and adapts Code Health to Deliverable Currency for non-code"
    - "sanity-check references auto memory (MEMORY.md) instead of stale LEARNINGS.md"
  artifacts:
    - path: "scaffold/templates/CLAUDE.md.tmpl"
      provides: "Non-code conditional sections for Work Stages and Session Patterns"
      contains: "Work Stages"
    - path: "scaffold/templates/STATE.md.tmpl"
      provides: "Non-code variant with domain-appropriate open items"
      contains: "recurring decisions"
    - path: "scaffold/skills/sanity-check/SKILL.md"
      provides: "Adaptive health check with Deliverable Currency for non-code"
      contains: "Deliverable Currency"
  key_links:
    - from: "scaffold/skills/sanity-check/SKILL.md"
      to: ".claude/settings.json env.KNZINIT_PROJECT_TYPE"
      via: "runtime env var read"
      pattern: "KNZINIT_PROJECT_TYPE"
---

<objective>
Add non-code project support to CLAUDE.md template, STATE.md template, and sanity-check skill.

Purpose: Non-code projects (research, writing, strategy) need templates that encode workflow processes rather than programming conventions, and health checks that assess deliverable currency rather than code health.

Output: Updated CLAUDE.md.tmpl with conditional non-code sections, updated STATE.md.tmpl with non-code variant fields, updated sanity-check with adaptive behavior based on KNZINIT_PROJECT_TYPE.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-non-code-and-skills/04-CONTEXT.md

<interfaces>
<!-- Key files being modified. Executor needs current content to make targeted edits. -->

From scaffold/templates/CLAUDE.md.tmpl (current, 62 lines):
- Has "## Project Structure" section at lines 57-59 with placeholder comment
- Ends with version marker <!-- knzinit v{{VERSION}} -->
- Uses {{PROJECT_NAME}}, {{PROJECT_DESCRIPTION}}, {{VERSION}} placeholders

From scaffold/templates/STATE.md.tmpl (current, 31 lines):
- Has "## Open Items" section with code-oriented items ("Establish tech stack")
- Has "## Session Continuity" section (used by hooks and /handoff)
- Ends with version marker

From scaffold/skills/sanity-check/SKILL.md (current, 60 lines):
- Section 1: "Code Health" with build/test/lint/debug artifact checks
- Section 2: "Documentation Currency" references stale LEARNINGS.md
- Section 3: "Context Handoff" verification
- Section 4: "Summary" table
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add non-code conditional sections to CLAUDE.md.tmpl and STATE.md.tmpl</name>
  <files>scaffold/templates/CLAUDE.md.tmpl, scaffold/templates/STATE.md.tmpl</files>
  <action>
**CLAUDE.md.tmpl changes:**

The SKILL.md orchestrator (updated in Plan 03) will read the template and conditionally adapt it based on project type. Add clear conditional markers so the orchestrator knows what to swap.

Replace the "## Project Structure" section (lines 57-61, the section with the placeholder comment) with TWO variant blocks separated by conditional comments:

```
<!-- IF code/unknown -->
## Project Structure

<!-- Update this section as the project takes shape -->
<!-- ENDIF -->

<!-- IF noncode -->
## Work Stages

<!-- Describe how deliverables move from draft to done. Example stages:
     Research/Gather -> Draft -> Review -> Final
     Update this as your workflow becomes clear. -->

## Session Patterns

<!-- Describe what a typical work session looks like.
     What should Claude default to doing at session start?
     What are the recurring tasks? -->
<!-- ENDIF -->
```

After the "## Key Files" section and before the conditional block above, add a "## Session Discipline" block that is non-code only:

```
<!-- IF noncode -->
## Session Discipline

Non-code projects lack git commits as natural save points. Before ending a session,
update `.planning/STATE.md` with current position, decisions made, and next steps.
This ensures the next session starts oriented without re-explanation.
<!-- ENDIF -->
```

Keep the version marker at the very bottom unchanged.

IMPORTANT: The conditional comment markers (<!-- IF code/unknown -->, <!-- IF noncode -->, <!-- ENDIF -->) are instructions to the SKILL.md orchestrator, not HTML that stays in the output. The orchestrator will strip the markers and include only the matching variant. This follows the Phase 2 pattern of "single template + conditional adaptation in SKILL.md."

**STATE.md.tmpl changes:**

Replace the "## Open Items" section content. Currently it has:
```
- [ ] Define project scope and requirements
- [ ] Establish tech stack (if code project)
```

Change to two conditional variants:

```
## Open Items

<!-- IF code/unknown -->
- [ ] Define project scope and requirements
- [ ] Establish tech stack (if code project)
<!-- ENDIF -->

<!-- IF noncode -->
- [ ] Define project scope and requirements
- [ ] Define deliverables and quality bar
- [ ] Map recurring decisions
- [ ] Document typical session workflow
<!-- ENDIF -->
```

Same conditional comment pattern as CLAUDE.md.tmpl -- orchestrator strips markers and includes matching variant.
  </action>
  <verify>
    <automated>grep -q "Work Stages" scaffold/templates/CLAUDE.md.tmpl && grep -q "Session Patterns" scaffold/templates/CLAUDE.md.tmpl && grep -q "Session Discipline" scaffold/templates/CLAUDE.md.tmpl && grep -q "IF noncode" scaffold/templates/STATE.md.tmpl && grep -q "recurring decisions" scaffold/templates/STATE.md.tmpl && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>CLAUDE.md.tmpl contains Work Stages, Session Patterns, and Session Discipline sections with conditional markers. STATE.md.tmpl contains non-code variant open items with conditional markers. Both templates retain version markers and all existing non-conditional content unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Make sanity-check adaptive and fix LEARNINGS.md reference</name>
  <files>scaffold/skills/sanity-check/SKILL.md</files>
  <action>
Rewrite the sanity-check SKILL.md to be adaptive based on project type. The skill reads `KNZINIT_PROJECT_TYPE` from `.claude/settings.json` env block at runtime.

**Section 1 replacement -- make adaptive:**

Replace the current "### 1. Code Health" section with an adaptive section:

```markdown
### 1. Project Health

**Detect project type:** Read `KNZINIT_PROJECT_TYPE` from `.claude/settings.json` env block. If missing or set to `code`/`unknown`, use code behavior. If set to `noncode`, use non-code behavior.

**Code projects (code/unknown/missing):**
- **Compilation/type check**: Run the project's build or type-check command
- **Test suite**: Run tests if a test framework is configured
- **Lint**: Run the linter if configured
- **Debug artifacts**: Scan for console.log/print statements, TODO/FIXME/HACK comments, hardcoded localhost URLs, hardcoded file paths containing /Users/ or /home/

If no build/test/lint commands are established yet, skip and note: "Code health checks will activate once build tooling is configured."

**Non-code projects (noncode):**
- **Deliverable Currency**: Are WIP deliverables referenced in STATE.md still active? Are open items still relevant?
- **Work Stages accuracy**: Does the "Work Stages" section in CLAUDE.md reflect where things actually are?
- **Stale references**: Are there references to completed or abandoned deliverables that should be archived?
```

**Section 2 fix -- replace LEARNINGS.md with auto memory:**

In "### 2. Documentation Currency", replace the line:
```
3. **`.planning/LEARNINGS.md`** — Were recent insights captured?
```
with:
```
3. **Auto memory (MEMORY.md)** — Is it populated? Are entries relevant and not stale?
```

**Section 4 fix -- update summary table:**

Change the summary table's first row from "Code Health" to "Project Health" to match the renamed section.

Keep the description frontmatter, the "## Behavior" heading, and sections 3 (Context Handoff) and "## When to Run" unchanged.
  </action>
  <verify>
    <automated>grep -q "Deliverable Currency" scaffold/skills/sanity-check/SKILL.md && grep -q "KNZINIT_PROJECT_TYPE" scaffold/skills/sanity-check/SKILL.md && grep -q "MEMORY.md" scaffold/skills/sanity-check/SKILL.md && ! grep -q "LEARNINGS.md" scaffold/skills/sanity-check/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>sanity-check SKILL.md reads KNZINIT_PROJECT_TYPE to switch between code health checks and Deliverable Currency checks for non-code projects. LEARNINGS.md reference replaced with auto memory (MEMORY.md) reference. Summary table reflects renamed section.</done>
</task>

</tasks>

<verification>
- CLAUDE.md.tmpl has both code and non-code conditional variants
- STATE.md.tmpl has both code and non-code conditional variants
- sanity-check is adaptive and references MEMORY.md not LEARNINGS.md
- All three files retain their version markers
- No content from code variant is lost -- it is preserved inside conditional markers
</verification>

<success_criteria>
- grep confirms Work Stages, Session Patterns, Session Discipline in CLAUDE.md.tmpl
- grep confirms non-code open items in STATE.md.tmpl
- grep confirms Deliverable Currency and KNZINIT_PROJECT_TYPE in sanity-check
- grep confirms LEARNINGS.md is gone from sanity-check
</success_criteria>

<output>
After completion, create `.planning/phases/04-non-code-and-skills/04-01-SUMMARY.md`
</output>
