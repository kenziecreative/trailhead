---
phase: 04-non-code-and-skills
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scaffold/skills/handoff/SKILL.md
  - scaffold/skills/resume/SKILL.md
autonomous: true
requirements: [SKIL-01, SKIL-02]

must_haves:
  truths:
    - "/handoff writes a 4-section structured summary to STATE.md Session Continuity section"
    - "/resume outputs 8-15 line concise orientation from STATE.md plus recent changes"
    - "/resume is a superset of SessionStart hook -- adds handoff summary, recent decisions, changes since last session"
    - "Both skills work for all project types (code, noncode, unknown)"
  artifacts:
    - path: "scaffold/skills/handoff/SKILL.md"
      provides: "/handoff skill that writes structured summary to STATE.md"
      contains: "Session Continuity"
    - path: "scaffold/skills/resume/SKILL.md"
      provides: "/resume skill that reads STATE.md and outputs orientation"
      contains: "orientation"
  key_links:
    - from: "scaffold/skills/handoff/SKILL.md"
      to: ".planning/STATE.md"
      via: "writes to Session Continuity section"
      pattern: "Session Continuity"
    - from: "scaffold/skills/resume/SKILL.md"
      to: ".planning/STATE.md"
      via: "reads STATE.md for orientation data"
      pattern: "STATE.md"
---

<objective>
Create /handoff and /resume scaffold skills for session state capture and orientation.

Purpose: /handoff ensures session state is captured to STATE.md before ending work. /resume provides richer orientation than the automatic SessionStart hook, pulling in handoff summary, recent decisions, and changes since last session.

Output: Two new skill SKILL.md files in scaffold/skills/ ready for installation during /knzinit scaffolding.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-non-code-and-skills/04-CONTEXT.md

<interfaces>
<!-- Existing patterns the executor needs to follow -->

From scaffold/skills/sanity-check/SKILL.md (established skill pattern):
```markdown
---
name: sanity-check
description: Verify project health before context clear, milestone completion, or deployment
---

# /sanity-check
[skill content]
```

From scaffold/templates/STATE.md.tmpl (Session Continuity section that /handoff writes to):
```markdown
## Session Continuity

### What a new session needs to know
- Project was just initialized with knzinit scaffolding
- ...
```

From scaffold/hooks/session-start.sh (SessionStart hook that /resume supersedes):
- Hook reads STATE.md and outputs basic orientation: current position, last activity
- /resume adds: handoff summary, recent decisions, changes since last session (git log)

From scaffold/hooks/hook-utils.sh (read_state_field function):
- Uses awk to extract sections between ## headings from STATE.md
- /resume can reference similar extraction approach
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /handoff skill</name>
  <files>scaffold/skills/handoff/SKILL.md</files>
  <action>
Create `scaffold/skills/handoff/SKILL.md` with the standard skill frontmatter pattern.

```markdown
---
name: handoff
description: Capture current session state to STATE.md for seamless session transitions
---

# /handoff — Session Handoff

Capture what happened this session so the next session (or a different context window) can pick up without re-explanation. This writes directly to STATE.md -- the single source of truth for session continuity.

## What to Capture

Write a **4-section structured summary** to the `## Session Continuity` section of `.planning/STATE.md`:

### 1. What was done this session
List completed work -- decisions made, files created/modified, problems solved. Be specific (file names, not "updated some files").

### 2. What's in progress
Anything started but not finished. Include enough context that the next session can continue without re-reading all the code.

### 3. What's next
Immediate next steps. Prioritize -- what should the next session tackle first?

### 4. Open questions/blockers
Unresolved questions, things waiting on user input, external dependencies, or known issues discovered this session.

## How to Write It

1. Read the current `.planning/STATE.md`
2. Replace the content under `## Session Continuity` with the 4-section summary above
3. Also update `## Current Position` to reflect where things actually are
4. Also update `## Last Activity` with today's date and a brief description
5. If any decisions were made this session, append them to the `## Recent Decisions` table

**Keep it concise.** Each section: 2-5 bullet points. The goal is orientation, not a full session transcript.

**Works for all project types** -- code, non-code, unknown. The 4-section structure is universal.

## When to Run

- Before ending a work session
- Before clearing context on a long session
- When switching to a different area of the project
- Anytime you want to checkpoint progress
```

Create the directory `scaffold/skills/handoff/` if it does not exist.
  </action>
  <verify>
    <automated>test -f scaffold/skills/handoff/SKILL.md && grep -q "name: handoff" scaffold/skills/handoff/SKILL.md && grep -q "Session Continuity" scaffold/skills/handoff/SKILL.md && grep -q "What was done" scaffold/skills/handoff/SKILL.md && grep -q "in progress" scaffold/skills/handoff/SKILL.md && grep -q "next" scaffold/skills/handoff/SKILL.md && grep -q "Open questions" scaffold/skills/handoff/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>/handoff SKILL.md exists with frontmatter, 4-section summary structure (done, in progress, next, open questions), writes to STATE.md Session Continuity section, works for all project types.</done>
</task>

<task type="auto">
  <name>Task 2: Create /resume skill</name>
  <files>scaffold/skills/resume/SKILL.md</files>
  <action>
Create `scaffold/skills/resume/SKILL.md` with the standard skill frontmatter pattern.

```markdown
---
name: resume
description: Read STATE.md and recent changes to output rich session orientation
---

# /resume — Session Resume

Output a concise orientation summary so you can pick up where the last session left off. This is a **superset of the SessionStart hook** -- the hook gives basic orientation (phase, position, last activity); /resume adds the handoff summary, recent decisions, and changes since last session.

Run this when the automatic SessionStart hook output is not enough context.

## What to Output

Read `.planning/STATE.md` and produce an **8-15 line orientation** covering:

1. **Current position** — from `## Current Position` section
2. **Last activity** — from `## Last Activity` section
3. **Session handoff** — from `## Session Continuity` section (the 4-part summary written by /handoff)
4. **Recent decisions** — last 3-5 entries from `## Recent Decisions` table
5. **Changes since last session** — if this is a git repo, run `git log --oneline -10` to show recent commits

## How to Format

Output as a compact bulleted summary. Example:

```
**Resuming: [project name]**

- Position: [current position from STATE.md]
- Last activity: [date and description]
- In progress: [from handoff summary]
- Next steps: [from handoff summary]
- Open questions: [from handoff summary, if any]
- Recent decisions: [last 2-3 decisions, one line each]
- Recent commits: [last 3-5 commit subjects, if git repo]
```

**Keep it to 8-15 lines.** This is a quick orientation, not a full project briefing. If the user wants more detail, they can read STATE.md directly.

## Handling Missing Data

- If `## Session Continuity` is empty or has only the initial placeholder: note "No handoff summary from previous session" and focus on position + decisions
- If not a git repo: skip the "Recent commits" line entirely
- If STATE.md does not exist: output "No STATE.md found. Run /handoff at the end of your next session to enable resume."

**Works for all project types** -- code, non-code, unknown. The orientation format is universal.

## When to Run

- At the start of a new session when the automatic hook output is insufficient
- When resuming after a long break
- When switching back to a project after working on something else
```

Create the directory `scaffold/skills/resume/` if it does not exist.
  </action>
  <verify>
    <automated>test -f scaffold/skills/resume/SKILL.md && grep -q "name: resume" scaffold/skills/resume/SKILL.md && grep -q "superset" scaffold/skills/resume/SKILL.md && grep -q "8-15 line" scaffold/skills/resume/SKILL.md && grep -q "git log" scaffold/skills/resume/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>/resume SKILL.md exists with frontmatter, outputs 8-15 line orientation, is superset of SessionStart hook (adds handoff summary, recent decisions, git log), handles missing data gracefully, works for all project types.</done>
</task>

</tasks>

<verification>
- Both skill files exist in scaffold/skills/{handoff,resume}/SKILL.md
- Both use standard frontmatter pattern (name, description)
- /handoff writes 4-section summary to STATE.md Session Continuity
- /resume reads STATE.md + git log and outputs concise orientation
- Both are project-type agnostic
</verification>

<success_criteria>
- scaffold/skills/handoff/SKILL.md exists with 4-section handoff structure
- scaffold/skills/resume/SKILL.md exists with 8-15 line orientation format
- Both reference STATE.md as the data source
- /resume explicitly described as superset of SessionStart hook
</success_criteria>

<output>
After completion, create `.planning/phases/04-non-code-and-skills/04-02-SUMMARY.md`
</output>
