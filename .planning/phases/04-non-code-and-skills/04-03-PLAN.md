---
phase: 04-non-code-and-skills
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - skills/knzinit/SKILL.md
autonomous: true
requirements: [NCODE-02]

must_haves:
  truths:
    - "SKILL.md interview branches after Step 1 -- non-code projects get Step 1B with 4 workflow-specific questions"
    - "Step 2 adapts for non-code -- skips language/framework detection and security tooling checks"
    - "Step 3A instructs orchestrator to use non-code conditional sections from templates"
    - "Step 3B installs /handoff and /resume skills alongside sanity-check"
    - "Not sure yet defaults to code variant (consistent with Phase 2 decision)"
  artifacts:
    - path: "skills/knzinit/SKILL.md"
      provides: "Updated orchestrator with non-code interview branch and skill installation"
      contains: "Step 1B"
  key_links:
    - from: "skills/knzinit/SKILL.md Step 1B"
      to: "scaffold/templates/CLAUDE.md.tmpl conditional sections"
      via: "project type drives template adaptation"
      pattern: "noncode"
    - from: "skills/knzinit/SKILL.md Step 3B"
      to: "scaffold/skills/handoff/SKILL.md"
      via: "skill installation during scaffolding"
      pattern: "handoff"
    - from: "skills/knzinit/SKILL.md Step 3B"
      to: "scaffold/skills/resume/SKILL.md"
      via: "skill installation during scaffolding"
      pattern: "resume"
---

<objective>
Update the /knzinit SKILL.md orchestrator with non-code interview branch (Step 1B), adapted Step 2 exploration, non-code template adaptation instructions, and /handoff + /resume skill installation.

Purpose: The orchestrator is the central control point that drives all conditional behavior. It needs to ask the right questions for non-code projects and install the new skills created in Plan 02.

Output: Updated skills/knzinit/SKILL.md with Step 1B branch, adapted Step 2, template adaptation instructions, and skill installation.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-non-code-and-skills/04-CONTEXT.md
@.planning/phases/04-non-code-and-skills/04-01-SUMMARY.md
@.planning/phases/04-non-code-and-skills/04-02-SUMMARY.md

<interfaces>
<!-- The executor needs the full current SKILL.md structure to make targeted edits -->

From skills/knzinit/SKILL.md (current structure, 201 lines):
- Frontmatter: name: knzinit, description: Bootstrap a new project...
- Step 1: Orientation (3 questions: what is project, code/non-code/unsure, git repo)
- Step 2: Explore What Exists (checks CLAUDE.md, .planning/, .claude/, language/framework, security tooling)
- Step 3: Build the Scaffolding
  - 3A: Two-System Architecture (CLAUDE.md from template, .claude/rules/)
  - 3B: Security & Sanity Setup (agents, hooks, sanity-check)
  - 3C: .gitignore
  - 3D: Create Directories
- Step 4: Merge Settings
- Step 5: Git Commit
- Step 6: Report

Key patterns:
- Step 3A already has: "If non-code or no git: add a Session Discipline section"
- Step 3B already has: "Only create security agents if this is a code project or the user is unsure"
- Step 3B already installs sanity-check: `scaffold/skills/sanity-check/SKILL.md` -> `.claude/skills/sanity-check/SKILL.md`

From Plan 01 output (templates):
- CLAUDE.md.tmpl has <!-- IF noncode --> and <!-- IF code/unknown --> conditional markers
- STATE.md.tmpl has same conditional markers for Open Items section
- Orchestrator must strip conditional markers and include only matching variant

From Plan 02 output (skills):
- scaffold/skills/handoff/SKILL.md -- /handoff skill
- scaffold/skills/resume/SKILL.md -- /resume skill
- Both use standard frontmatter pattern, installed to .claude/skills/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Step 1B non-code interview branch</name>
  <files>skills/knzinit/SKILL.md</files>
  <action>
Make THREE changes to skills/knzinit/SKILL.md:

**Change 1: Add Step 1B after Step 1**

Insert a new section between Step 1 and Step 2:

```markdown
## Step 1B: Non-Code Workflow Questions (non-code projects only)

If the user answered "non-code" in Step 1, ask these additional questions before proceeding to Step 2. **All accept "not sure yet" as a valid answer.** Skip this step entirely for code projects and "not sure yet."

1. **What does a typical work session look like?** (e.g., "I research for 30 min then draft for an hour", "I review documents and leave comments")
2. **What are your main deliverables/outputs?** (e.g., reports, strategies, analyses, documentation)
3. **What decisions come up repeatedly?** (e.g., "which sources to prioritize", "how to structure recommendations")
4. **Any domain terminology Claude should know?** (e.g., industry jargon, acronyms, process names)

Use these answers to populate the Work Stages and Session Patterns sections in CLAUDE.md and the open items in STATE.md. If answers are vague or "not sure yet", use the template defaults.

Wait for answers before proceeding.
```

**Change 2: Adapt Step 2 for non-code**

In Step 2 ("Explore What Exists"), add a note after the existing bullet list:

```markdown
**Non-code adaptation:** If this is a non-code project, skip the last two bullets (language/framework detection and security tooling checks). Instead, check:
- Does the project have existing deliverables or documents?
- Is there an existing workflow or process documented anywhere?
```

**Change 3: Update Step 3 for template adaptation and skill installation**

In Step 3A, after the existing CLAUDE.md paragraph that mentions creating from template, add:

```markdown
**Template adaptation for non-code:** The templates (`CLAUDE.md.tmpl`, `STATE.md.tmpl`) contain conditional sections marked with `<!-- IF code/unknown -->` and `<!-- IF noncode -->` comment markers. When generating output:
- For **code** or **not sure yet** projects: include only `<!-- IF code/unknown -->` sections, strip all conditional markers
- For **non-code** projects: include only `<!-- IF noncode -->` sections, strip all conditional markers
- The conditional markers themselves must NOT appear in the generated output

For non-code projects, populate the Work Stages and Session Patterns sections using answers from Step 1B. If the user said "not sure yet" to those questions, keep the template placeholder comments.
```

In Step 3B, after the existing sanity-check installation line, add installation for /handoff and /resume:

```markdown
**All projects:** Install handoff and resume skills:
- `scaffold/skills/handoff/SKILL.md` -> `.claude/skills/handoff/SKILL.md`
- `scaffold/skills/resume/SKILL.md` -> `.claude/skills/resume/SKILL.md`
```

In Step 6 (Report), after the existing reminder about `/sanity-check`, add:

```markdown
- Remind them that `/handoff` captures session state and `/resume` restores orientation
```
  </action>
  <verify>
    <automated>grep -q "Step 1B" skills/knzinit/SKILL.md && grep -q "typical work session" skills/knzinit/SKILL.md && grep -q "Non-code adaptation" skills/knzinit/SKILL.md && grep -q "IF noncode" skills/knzinit/SKILL.md && grep -q "scaffold/skills/handoff/SKILL.md" skills/knzinit/SKILL.md && grep -q "scaffold/skills/resume/SKILL.md" skills/knzinit/SKILL.md && grep -q "/handoff" skills/knzinit/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>SKILL.md has Step 1B with 4 workflow questions for non-code projects. Step 2 has non-code adaptation note. Step 3A has template conditional adaptation instructions. Step 3B installs /handoff and /resume skills. Step 6 mentions new skills. "Not sure yet" still defaults to code variant.</done>
</task>

</tasks>

<verification>
- Step 1B exists with 4 questions matching CONTEXT.md decisions
- Step 1B is skipped for code/"not sure yet" projects
- Step 2 adapts for non-code (skip language/framework/security checks)
- Step 3A has template conditional adaptation instructions
- Step 3B installs handoff and resume skills for all projects
- Step 6 mentions /handoff and /resume
- Existing Step 1 questions unchanged
- "Not sure yet" defaults to code variant throughout
</verification>

<success_criteria>
- grep confirms Step 1B, non-code adaptation, template conditionals, handoff/resume installation
- SKILL.md remains coherent as a single orchestration document
- Non-code interview flow: Step 1 -> Step 1B -> Step 2 (adapted) -> Step 3+
- Code/unsure flow: Step 1 -> Step 2 (unchanged) -> Step 3+
</success_criteria>

<output>
After completion, create `.planning/phases/04-non-code-and-skills/04-03-SUMMARY.md`
</output>
