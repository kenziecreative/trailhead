---
phase: 05-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scaffold/templates/mcp.json.tmpl
  - scaffold/templates/decisions-archive.md.tmpl
  - skills/knzinit/SKILL.md
  - scaffold/skills/sanity-check/SKILL.md
autonomous: true
requirements: [INFR-03, INFR-04]

must_haves:
  truths:
    - "When /knzinit asks about external services and user says yes, a .mcp.json template is generated alongside settings.json"
    - "When /knzinit asks about external services and user says not sure yet, no .mcp.json is generated"
    - "enableAllProjectMcpServers is added to settings.json only when .mcp.json was generated"
    - "Every scaffolded project gets a decisions-archive.md file in .planning/"
    - "/sanity-check warns when STATE.md decisions exceed 20 entries with actionable message"
  artifacts:
    - path: "scaffold/templates/mcp.json.tmpl"
      provides: "Empty MCP config template with comments explaining format"
      contains: "mcpServers"
    - path: "scaffold/templates/decisions-archive.md.tmpl"
      provides: "Decisions archive with same table format as STATE.md"
      contains: "Decision"
    - path: "skills/knzinit/SKILL.md"
      provides: "Updated orchestrator with Q4, .mcp.json generation, decisions-archive creation, enableAllProjectMcpServers merge"
      contains: "external services"
    - path: "scaffold/skills/sanity-check/SKILL.md"
      provides: "Updated sanity-check with decisions overflow warning"
      contains: "decisions"
  key_links:
    - from: "skills/knzinit/SKILL.md"
      to: "scaffold/templates/mcp.json.tmpl"
      via: "Step 3A references template for .mcp.json generation"
      pattern: "mcp.json"
    - from: "skills/knzinit/SKILL.md"
      to: "scaffold/templates/decisions-archive.md.tmpl"
      via: "Step 3A references template for decisions-archive creation"
      pattern: "decisions-archive"
    - from: "skills/knzinit/SKILL.md"
      to: "scaffold/templates/settings.json.tmpl"
      via: "Step 4 conditionally adds enableAllProjectMcpServers"
      pattern: "enableAllProjectMcpServers"
    - from: "scaffold/skills/sanity-check/SKILL.md"
      to: ".planning/STATE.md"
      via: "Section 2 counts decisions entries"
      pattern: "decisions.*20"
---

<objective>
Add MCP template generation and decisions archive scaffolding to the /knzinit workflow.

Purpose: Projects using external services (APIs, databases, Slack, Notion) need an MCP configuration template. All projects need a decisions archive file so STATE.md decisions can be rotated when they exceed the 20-entry cap. The sanity-check skill needs to detect and warn about overflow.

Output: Two new templates (mcp.json.tmpl, decisions-archive.md.tmpl), updated /knzinit orchestrator with interview Q4 and conditional generation logic, updated sanity-check with overflow detection.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-infrastructure/05-CONTEXT.md

<interfaces>
<!-- Key structures the executor needs from existing files -->

From scaffold/templates/STATE.md.tmpl (decisions table format to match):
```markdown
| # | Decision | Date | Context |
|---|----------|------|---------|
| 1 | Project scaffolding initialized | {{DATE}} | /knzinit bootstrap |

> Keep max 20 entries. Archive older decisions to `.planning/decisions-archive.md`.
```

From scaffold/templates/settings.json.tmpl (merge target for enableAllProjectMcpServers):
```json
{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": { ... },
  "env": {
    "KNZINIT_PROJECT_TYPE": "code",
    "KNZINIT_VERSION": "{{VERSION}}"
  },
  "plansDirectory": ".planning/plans",
  "hooks": { ... }
}
```

From skills/knzinit/SKILL.md (current Step 1 structure — 3 questions):
```markdown
## Step 1: Orientation
Ask the user these three questions. **All three accept "not sure yet" as a valid answer.**
1. **What is this project?**
2. **Is this a code project, a non-code project, or not sure yet?**
3. **Is this already a git repository?**
```

From skills/knzinit/SKILL.md Step 4 (merge logic location):
```markdown
## Step 4: Merge Settings
The canonical template is at `${KNZINIT_ROOT}/scaffold/templates/settings.json.tmpl`.
...
**Scalar keys** (`$schema`, `plansDirectory`): Set if not already present; do not overwrite existing values.
```

From scaffold/skills/sanity-check/SKILL.md (Section 2 — target for overflow check):
```markdown
### 2. Documentation Currency
Read and verify these files are current (check whichever exist):
1. **`.planning/STATE.md`** — Does the current position match reality? Is the last activity date current?
2. **`CLAUDE.md`** — Does it reflect the actual project state?
3. **Auto memory (MEMORY.md)** — Is it populated? Are entries relevant and not stale?
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mcp.json.tmpl and decisions-archive.md.tmpl templates</name>
  <files>scaffold/templates/mcp.json.tmpl, scaffold/templates/decisions-archive.md.tmpl</files>
  <action>
Create two new template files:

**scaffold/templates/mcp.json.tmpl:**
- Empty JSON structure with `mcpServers` object containing no servers
- Include JSON comments (using // style that Claude Code settings support, or use a commented-out example server entry) explaining:
  - How to add MCP servers (name, command, args, env pattern)
  - Link to MCP documentation or reference
  - Note that this file configures project-level MCP servers
- Include `{{VERSION}}` version marker as a comment at the bottom
- Keep it minimal — this is a starting point the user fills in, not a comprehensive config

**scaffold/templates/decisions-archive.md.tmpl:**
- Header: `# Decisions Archive`
- Brief description: "Archived decisions from STATE.md. Newest entries at top."
- Same table format as STATE.md decisions table: `| # | Decision | Date | Context |`
- Empty table (header row + separator only, no data rows)
- Include version marker `<!-- knzinit v{{VERSION}} -->` at the bottom
- Keep under 20 lines total
  </action>
  <verify>
    <automated>test -f scaffold/templates/mcp.json.tmpl && test -f scaffold/templates/decisions-archive.md.tmpl && grep -q "mcpServers" scaffold/templates/mcp.json.tmpl && grep -q "Decision" scaffold/templates/decisions-archive.md.tmpl && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Both template files exist with correct structure: mcp.json.tmpl has mcpServers key with explanatory comments, decisions-archive.md.tmpl has matching table format and version marker</done>
</task>

<task type="auto">
  <name>Task 2: Update SKILL.md orchestrator and sanity-check with MCP generation and decisions overflow</name>
  <files>skills/knzinit/SKILL.md, scaffold/skills/sanity-check/SKILL.md</files>
  <action>
**skills/knzinit/SKILL.md — four targeted edits:**

1. **Step 1 (Orientation):** Change "these three questions" to "these four questions" and add question 4 after the existing question 3:
   ```
   4. **Does this project use external services (APIs, databases, Slack, etc.)?** (Yes, no, or not sure yet.)
   ```
   This question is asked for ALL project types (code, non-code, unknown). Per locked decision: "not sure yet" skips .mcp.json generation.

2. **Step 3A (Two-System Architecture):** Add two new items to the scaffolding instructions:

   Under the "Learning System" subsection, after the STATE.md creation paragraph, add:
   ```
   **decisions-archive.md** — Create from `${KNZINIT_ROOT}/scaffold/templates/decisions-archive.md.tmpl` in `.planning/decisions-archive.md`. Scaffolded for every project. Substitute `{{VERSION}}` with the current version.
   ```

   Under the config files area (after settings references but within Step 3A), add a conditional .mcp.json block:
   ```
   **.mcp.json** (conditional) — If the user answered "yes" to the external services question (Step 1, Q4), create `.mcp.json` in the project root from `${KNZINIT_ROOT}/scaffold/templates/mcp.json.tmpl`. Substitute `{{VERSION}}` with the current version. If the user answered "no" or "not sure yet", skip this file.
   ```

3. **Step 4 (Merge Settings):** After the existing scalar keys merge rule paragraph, add a conditional paragraph:
   ```
   **MCP integration (conditional):** If `.mcp.json` was generated in Step 3A (user answered "yes" to external services), add `"enableAllProjectMcpServers": true` as a top-level scalar key in settings.json using the set-if-absent rule. If no .mcp.json was generated, do not add this key.
   ```

4. **Step 6 (Report):** In the "List every file created" guidance, add mentions of:
   - `.planning/decisions-archive.md` (always created)
   - `.mcp.json` (if created — note it was generated because user indicated external services)
   - If .mcp.json was created, remind user to populate it with their actual MCP server configurations

**scaffold/skills/sanity-check/SKILL.md — one targeted edit:**

In Section 2 (Documentation Currency), after the existing STATE.md check (item 1), add a sub-check:
```
   - **Decisions overflow:** Count the entries in the Recent Decisions table. If more than 20 entries, warn: "STATE.md has {N} decisions (cap: 20). Archive the oldest {N-20} to `.planning/decisions-archive.md` to keep STATE.md lean."
```

This goes inside the existing STATE.md bullet as a sub-item, since decisions are part of STATE.md health. The check is actionable — it tells the user exactly how many to archive and where.
  </action>
  <verify>
    <automated>grep -q "external services" skills/knzinit/SKILL.md && grep -q "decisions-archive" skills/knzinit/SKILL.md && grep -q "enableAllProjectMcpServers" skills/knzinit/SKILL.md && grep -q "mcp.json" skills/knzinit/SKILL.md && grep -q "decisions" scaffold/skills/sanity-check/SKILL.md && grep -q "20" scaffold/skills/sanity-check/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>SKILL.md has Q4 in Step 1, .mcp.json conditional generation in Step 3A, decisions-archive.md creation in Step 3A, enableAllProjectMcpServers conditional merge in Step 4, both files mentioned in Step 6. Sanity-check SKILL.md Section 2 warns on >20 decisions with actionable message including count and archive target.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full integration:

1. `scaffold/templates/mcp.json.tmpl` exists and contains mcpServers structure
2. `scaffold/templates/decisions-archive.md.tmpl` exists with matching table format
3. `skills/knzinit/SKILL.md` contains all four integration points: Q4, Step 3A .mcp.json, Step 3A decisions-archive, Step 4 enableAllProjectMcpServers
4. `scaffold/skills/sanity-check/SKILL.md` Section 2 includes decisions overflow check with 20-entry cap
5. All version marker patterns ({{VERSION}}) present in new templates
</verification>

<success_criteria>
- Two new templates created following established .tmpl naming convention
- /knzinit interview expanded from 3 to 4 questions, with Q4 asked for all project types
- .mcp.json generation is conditional on "yes" answer only — "no" and "not sure yet" both skip
- decisions-archive.md is scaffolded for every project unconditionally
- enableAllProjectMcpServers added to settings only when .mcp.json exists (set-if-absent merge rule)
- sanity-check detects >20 decisions with actionable warning message
- Step 6 report mentions both new files
</success_criteria>

<output>
After completion, create `.planning/phases/05-infrastructure/05-01-SUMMARY.md`
</output>
